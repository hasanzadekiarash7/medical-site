export type packageCard = {
  imageLink: string;
  title: string;
};

export type treatmentCard = {
  imageLink: string;
  title: string;
  description: string;
};

export type infoCard = {
  Icon: string;
  title: string;
  description: string;
};
